;;; wgrep-config --- Summary  -*- lexical-binding: t; -*-
;;; Commentary:
;;; Code:
(use-package wgrep
  :ensure t
  :config
  (custom-set-variables
   '(wgrep-auto-save-buffer t))
  )

(provide 'wgrep-config)
;;; wgrep-efap-config.el ends here
