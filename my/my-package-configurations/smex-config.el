;;; smex-config --- Summary  -*- lexical-binding: t; -*-
;;; Commentary:
;;; Code:
(use-package smex
  :ensure t
  :bind (("M-x" . smex)
         ("M-X" . smex-major-mode-commands)))

(provide 'smex-config)
;;; smex-config.el ends here
